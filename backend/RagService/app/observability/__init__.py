"""Cross-cutting observability: structured logging, correlation ids, and metrics.

Sits alongside the clean-architecture layers as a shared concern (like stdlib
logging) that the application and API layers may both use.
"""
